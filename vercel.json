{
  "version": 2,
  "buildCommand": "cd frontend && npm run build",
  "outputDirectory": "frontend/.next",
  "installCommand": "npm run build:packages && cd frontend && npm install",
  "framework": "nextjs"
}
