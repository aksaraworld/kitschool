{
  "version": 2,
  "buildCommand": "if [ -f build-packages.js ]; then node build-packages.js && cd frontend; else cd .. && node build-packages.js && cd frontend; fi && npm install && npm run build",
  "outputDirectory": ".next",
  "installCommand": "if [ -f ../package.json ] && [ ! -f build-packages.js ]; then cd .. && npm install; else npm install; fi",
  "framework": "nextjs"
}
